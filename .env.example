# choose provider: "google" or "openai" (llm-client supports both shapes)
LLM_PROVIDER=
# For Google GenAI REST endpoint (or your chosen endpoint)
LLM_API_URL=
LLM_API_KEY=
# model name to call (adjust to provider)
LLM_MODEL=
# optional: maximum allowed input chars (sanity)
MAX_INPUT_CHARS=
